## 1. 插件系统基础能力扩展

- [x] 1.1 在 `packages/shared/src/types/plugin.ts` 新增 AI 扫描索引、任务控制、文档会话桥接的类型契约（请求/响应/事件）。
- [x] 1.2 在主进程插件运行时新增 `PluginTaskRuntime`（状态机、任务句柄、事件分发）并接入现有插件生命周期。
- [x] 1.3 在主进程新增 `PluginDocumentChatBridge` 服务，提供按 `pluginId + knowledgeBaseId + sessionId` 隔离的会话与流式输出。
- [x] 1.4 在 `apps/electron/src/main/ipc.ts` 与 `apps/electron/src/preload/index.ts` 扩展统一 API：扫描启动/暂停/继续/停止/状态查询、文档会话发送/订阅/结束。

## 2. AI 扫描与索引流水线落地

- [x] 2.1 在插件宿主侧实现可复用 AI 扫描流水线：仓库遍历 -> 语义分块 -> Embedding -> 索引持久化到插件工作区。
- [x] 2.2 设计并实现索引元数据契约（模型、分块策略、索引版本、仓库指纹、生成时间）与加载校验逻辑。
- [x] 2.3 实现增量复用策略：同仓库重复扫描时优先复用有效索引分片，仅重建变更部分。
- [x] 2.4 为扫描流水线接入模型选择参数与可用性校验，确保不可用模型给出可诊断错误。

## 3. Wiki 插件能力升级

- [x] 3.1 改造 `wiki-local-repository-plugin` 的 `analyze` 动作为调用宿主 AI 扫描标准能力，不再仅输出统计型 Markdown。
- [x] 3.2 在 Wiki 插件画布中新增任务控制动作（pause/resume/stop）与运行态展示（进度、阶段、错误）。
- [x] 3.3 在 Wiki 插件画布中新增模型选择输入并将选择结果透传到扫描任务。
- [x] 3.4 在 Wiki 插件中接入“继续聊天”入口：调用文档会话桥接，展示流式回答与引用。
- [x] 3.5 保留并兼容 `get-latest` 能力，支持读取最新 Markdown 与索引元数据摘要。

## 4. Plugin 工作台壳层与画布协议升级

- [x] 4.1 扩展 `plugin-ui-canvas-hook` 协议，支持任务控制动作定义与文档会话区块定义。
- [x] 4.2 在 `plugin-workbench-shell` 实现统一任务状态条与控制按钮，按协议驱动而非 Wiki 插件特判。
- [x] 4.3 在渲染层实现文档会话消息流通用组件（输入、输出、引用、错误态），供 Wiki 及后续插件复用。
- [x] 4.4 完善降级策略：插件未声明新协议时保持现有占位/旧能力可用。

## 5. 稳定性、安全与验收

- [x] 5.1 为任务控制状态机补充单元测试（非法状态转换、幂等命令、事件顺序）。
- [x] 5.2 为 AI 扫描与索引服务补充集成测试（索引生成、增量复用、元数据校验失败）。
- [x] 5.3 为文档会话桥接补充集成测试（会话隔离、流式事件完整性、异常收敛）。
- [x] 5.4 为 Wiki 工作台补充关键 E2E 流程：分析->暂停->继续->完成->继续聊天。
- [x] 5.5 执行项目 lint/typecheck（按仓库规范）并修复新增问题。

## 6. 文档与迁移说明

- [x] 6.1 更新 `PLUGIN.md`：新增“插件系统基础能力”章节（AI 扫描索引、任务控制、文档会话桥接）及接入约定。
- [x] 6.2 更新 `PLUGIN_WIKI.md`：补充模型选择、任务控制、继续聊天与索引产物说明。
- [x] 6.3 编写插件迁移指引：说明旧版仅扫描插件如何渐进接入新协议（兼容路径与最小改造步骤）。
- [x] 6.4 在 OpenSpec 变更中补充实现后核对清单，确保 proposal/design/specs 与代码行为一致。

## 实现后核对清单（6.4）

- [x] proposal 中“任务控制 + 文档会话桥接 + 模型选择”已在代码中落地（`PluginTaskRuntime`、`PluginDocumentChatBridge`、Wiki 工作台模型输入）。
- [x] design 中“宿主下沉基础能力、插件侧消费”的决策已落地（插件通过 `context.api.aiIndexing` 与 `context.api.documentChat` 调用宿主）。
- [x] specs 中新增协议节点已落地（`task-status`、`document-chat`）并由统一壳层渲染。
- [x] specs 中 API 扩展已落地（任务控制与文档会话 IPC + preload 统一入口）。
- [x] 测试覆盖与变更目标一致（任务状态机、AI 索引增量复用与元数据校验、文档会话隔离与事件完整性、Wiki 关键流程）。
